// STUB — fully EN until Urdu translations are filled in by next-wave agent.
// Note: ur is RTL — RTL layout pass deferred to dedicated push.
import en from './en'

const ur: typeof en = en

export default ur
