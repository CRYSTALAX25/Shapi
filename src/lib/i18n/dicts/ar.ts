// STUB — fully EN until Arabic translations are filled in by next-wave agent.
// Note: ar is RTL — RTL layout pass deferred per Ana's call (Arabic ships LAST after full build + testing).
import en from './en'

const ar: typeof en = en

export default ar
